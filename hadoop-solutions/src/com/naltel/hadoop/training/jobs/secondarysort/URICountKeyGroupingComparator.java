package com.naltel.hadoop.training.jobs.secondarysort;
/*

import org.apache.hadoop.io.RawComparator;
import org.apache.hadoop.io.WritableComparator;

public class URICountKeyGroupingComparator implements RawComparator<URICountKey> {

	@Override
	public int compare(byte[] b1, int s1, int l1, byte[] b2, int s2, int l2) {
		return WritableComparator.compareBytes(b1, s1, l1 - Integer.SIZE / 8, b2, s2, l2 - Integer.SIZE / 8);
	}

	@Override
	public int compare(URICountKey o1, URICountKey o2) {
		return o1.getUri().compareTo(o2.getUri());
	}
}
*/
